-   expliquer l'arborence ada -> leur faire creer un dossier ada dans leur home
-   expliquer la notion de home
-   expliquer les conventions de nommage (pas d'espaces, pas de majuscules) -> nom de dossiers en kebab-case
-   ajouter la commande `code` (install + usage)

mkdir les-bases-du-terminal

echo "<h1>Mon premier site web !</h1>" > index.html
npx live-server

Ctrl+C
