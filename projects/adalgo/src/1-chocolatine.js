/**
 * This function takes a message as parameter and replaces all occurences
 * of "pain au chocolat" by "chocolatine".
 *
 * @param message {string} The message in which the text should be replaced
 * @returns an updated message with every "pain au chocolat" replaced by "chocolatine"
 */
export const chocolatine = (message) => {};
